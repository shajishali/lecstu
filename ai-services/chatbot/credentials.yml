# LECSTU Chatbot Channels — Phase 8.1
# REST channel for frontend chat widget

rest:
  # POST /webhooks/rest/webhook
  # Request: { "sender": "user_id", "message": "text" }
  # Response: [{ "text": "..." }]
