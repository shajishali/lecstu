# LECSTU Chatbot Action Server — Phase 8.1
# Run: rasa run actions

action_endpoint:
  url: "http://localhost:5055/webhook"
  # Allow custom actions (lecturer lookup + availability) to finish without Rasa aborting early
  request_timeout: 60
